module.exports = {
  moduleDirectories: ['src', 'node_modules'],
  modulePathIgnorePatterns: ['coverage', 'lib'],
  preset: 'ts-jest',
  testEnvironment: 'node',
  testPathIgnorePatterns: ['coverage', 'lib'],
}
